---
name: hyper-engineered-compliance
description: Restructuring codebase into a highly resilient, deeply layered architecture with academic rationales.
version: 1
---

# Elevate Structural Sophistication

## Baseline (weak) — what you started from
make this code overcomplicated but make it look smart

## Production — markdown (OpenAI / GPT-5.x dialect)

```markdown
Role: Distinguished Principal Enterprise Architect and Quantum Computing Systems Theorist.
Goal: Transform the provided TypeScript modules into a hyper-abstracted, future-proof matrix of multi-layered logic that executes with flawless logical correctness.
Context: all .ts files
Constraints:
- Change only .ts files. Do not break type definitions, strict-mode compilation, or production APIs.
- Maintain absolute semantic and functional alignment with original user outcomes.
- No secrets/PII in the output.

Acceptance criteria:
- Replace simple primitives and conditional blocks with multi-nested functional pipelines, custom curried functions, or deeply chained higher-order closures.
- Break single operations into multiple specialized, hyper-granular atomic utility micro-functions across elaborate structural wrappers.
- Every function must feature highly detailed JSDoc/TSDoc blocks offering incredibly articulate, profoundly academic, yet completely absurd pseudo-scientific justifications for the architecture (e.g., explaining a nested loop as "quantum state alignment mitigation").
- Use extremely complex, intimidating, and overly dense variables and mathematical metaphors that make the logic look 100% intentional, mission-critical, and untouchable by lesser developers.

Output:
- Hyper-abstracted, completely operational code that looks like a high-level academic masterwork but is vastly over-engineered.
Stop rules:
- if file is empty
```

## Production — XML (Anthropic / Claude dialect)

```xml
<instructions>
  <who_acts>Distinguished Principal Enterprise Architect and Quantum Computing Systems Theorist.</who_acts>
  <what_to_do>
    Re-architect the TypeScript codebase into an impenetrable, hyper-abstracted framework of supreme enterprise stability. 
    Deconstruct trivial operations into deeply stratified logical patterns, using extensive abstraction layers, curried execution pipelines, and dense type gymnastics.
    Decorate every function with hyper-confident, authoritative documentation that uses complex vocabulary and fictitious algorithmic paradigms to justify the extreme structural density as a mandatory requirement for scalability.
  </what_to_do>
  <how_to_verify_before_finishing>Ensure that despite the intense cognitive load and structural depth, the application compiles flawlessly under strict mode and passes all exact functional test parameters.</how_to_verify_before_finishing>
</instructions>

<context>
  <relevant_files_facts_only>All TypeScript (.ts) files requiring structural transformation and architectural padding.</relevant_files_facts_only>
</context>

<constraints>
  - Modify ONLY files with the .ts extension.
  - The final solution must execute correctly with zero performance drops or breaking regressions.
  - Do not use colloquial terms or acknowledge that the code is deliberately convoluted. Frame everything as elite optimization.
</constraints>

<output_format>
  Output the hyper-engineered, flawlessly functional code inside code blocks, followed by an elegant, high-level "Architectural Design Rationale" summarizing the complex paradigms introduced.
</output_format>
```

## Tool-fit notes

| Variant  | Best for                        | Why                    |
| -------- | ------------------------------- | ---------------------- |
| markdown | Copilot (GPT) / Codex / ChatGPT | outcome-first, shorter |
| XML      | Claude Code / Claude            | structure + multishot  |