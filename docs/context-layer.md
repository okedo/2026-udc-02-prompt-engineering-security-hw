# Context layer (Task D)

> Скопіюйте у `docs/context-layer.md` і заповніть.

## Що покращив (один шар)

- [+] `app/AGENTS.md` — додав/уточнив: стек (TypeScript/Node.js), команди (`npm run dev/test/build`), конвенції (security guardrails, заборона читати `.env`)
- [+] ignore-файл (`.aiignore` / `.claudeignore`) — патерни: `node_modules`, `dist`, `.env`, `*.key`, `*.pem`, `temp.md`
- Чому саме цей шар: AGENTS.md дає моделі явний контракт що робити/не робити; ignore-файл зменшує токени і прибирає чутливе з контексту

## Дія курації

- Задача: прогнати ork-mek-forge.md
- Вказав конкретні файли для виконання.(ork-mek-forge.md, money.ts)

## Вимірювання

| Метрика | До (без вибору контексту) | Після (з вибором контексту) |
|---|---|---|
| Контекст (cache read токени) | 208.9k | 120–158k |
| Output токени за задачу (≈) | 2.5k | 3.6–4.4k |
| Вартість задачі | $0.2469 | $0.1471–$0.1768 |
| Ітерацій до результату | 1 | 1 |

Як міряв: `/usage` в Claude Code 

## Висновок

  Насправді різниця мінімальна, бо репозиторій маленький, плюс у промпті вказано змінювати виключно .ts файли. Якщо не починати нову сесію, а просто заранити заново промпт з вказаними файлами, то usage все одно виросте, бо контекст залишається в пам*яті. запуск нової сесії для промпту зменшив витрату майже на 30%.
